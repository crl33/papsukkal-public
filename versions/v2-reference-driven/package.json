{
  "name": "papsukkal-v2-reference-driven",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "V2 — Reference-Driven Living Photograph. The reference artwork itself, decomposed into layers and animated by the V1 breeze model.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "assets": "node tools/prepare-assets.mjs",
    "shots": "node scripts/shots.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/three": "0.185.0",
    "sharp": "^0.34.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
